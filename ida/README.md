# IDA Pro interop source

`import_mobile_ghidra.py` is copied into every V5.5 human artifact by `tools/v55_ida_pack.py`.

It is annotation-only by default: comments enabled, strong/proven suggested renames opt-in, no byte patching, no function creation, and no function-boundary changes. The main pipeline has no IDA dependency.
